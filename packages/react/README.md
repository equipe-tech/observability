# @equipe-tech/observability-react

Adaptador de observabilidade para aplicações React no browser.

A aplicação mantém a propriedade do root, do contexto, da interface de fallback, do roteador e do unmount. O pacote fornece `createBrowserObservability`, callbacks estruturais do React 19 e `runBrowserDeliveryCanary`.
